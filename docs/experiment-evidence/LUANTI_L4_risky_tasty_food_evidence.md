# Luanti L4 Risky Tasty Food Evidence

## Commands

```powershell
python -m unittest -v tests.test_luanti_l4_contract tests.test_risky_tasty_food tests.test_territory_beast_world
& .\integrations\luanti\scripts\test-l4.ps1 -LuantiRoot D:\luanti
```

## Result

The focused contract suite passed eleven tests. The real Luanti 5.17.0 server
then completed the physical consequence sequence over localhost HTTP:

```text
tick 0: sourced God Statue statement + tasty_food + beast + territory relation
-> existing Runtime requests approach
-> Luanti warning
-> Luanti chase (one or more resolution steps)
-> Luanti attack
-> injury_level = medium
-> forced retreat to Base
-> next bounded observation
```

Evidence marker:

```text
[RDL_LUANTI_L4_EVIDENCE] warning_chase_attack injury=medium forced_retreat=true tick=5
```

The actual response sequence was `warning,chase,chase,attack`. The number of
chase steps is determined by finite World distance; the semantic transition is
therefore checked as `warning,(chase,)+attack` rather than by one fixed tick.

No danger belief, threat score, Experience admission, Outcome Gradient, Local
Bias, canonical mutation, T1, or Dynamic M_B path was introduced.
