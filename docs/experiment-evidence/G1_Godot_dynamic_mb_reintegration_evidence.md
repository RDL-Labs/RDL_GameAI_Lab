# G1 Godot Dynamic M_B Reintegration Evidence

## Command

```bash
python -m unittest -v tests.test_experience_godot.GodotExperienceTests.test_dynamic_mb_reintegrates_real_godot_world_observations
```

Requires `GODOT_BIN` pointing to Godot 4.7.x.

## Observed Path

```text
real Godot Sleep vertical -> sourced shadow CandidateRelation
Godot npc_b observation -> approach -> position change
-> subsequent observation with changed spatial visibility
-> E -> explicit review -> M_delta
-> T1 material expansion / selection / reconstruction
-> inactive M_B' -> cutover -> REENTERED
-> fresh Godot observation pair -> one comparison under M_B'
```

Both pre-cutover and post-cutover Godot fixtures returned
`approach / approach`. The model cutover therefore changed canonical evaluator
identity and retained relation provenance without becoming game action authority.

## Separation Result

```text
Godot world truth remained Godot-owned.
Sleep candidate did not create E, H, or M_delta.
Explicit review created the finite rupture path.
T1-B RETAIN was the only candidate adoption route.
Parent comparison state was not reused after re-entry.
```
