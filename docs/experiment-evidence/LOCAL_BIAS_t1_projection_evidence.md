# Local Bias T1 Candidate Projection Evidence

## Command

```bash
python -m unittest -v tests.test_local_bias_t1_projection tests.test_local_bias_deep_similarity
```

## Result

One mixed Local Bias shadow candidate was projected into stable, independently
inspectable CandidateRelation children. Positive reward and negative injury
remained distinct and retained the same parent Candidate, Experience, Bias,
World event, sleep-cycle, and support provenance.

The projected children were then supplied explicitly to an active same-agent
T1-A expansion. Every child entered the immutable bundle as `UNINSPECTED`; no
selection, adoption, reconstruction, `M_B` mutation, or action influence was
performed. Invalid source identity and duplicate relation signatures were
rejected.

The focused OGB-7 through OGB-9 regression completed 9 tests successfully.
