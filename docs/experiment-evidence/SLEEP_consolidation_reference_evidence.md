# Sleep Consolidation S4 Reference Evidence

**Boundary:** real Godot / HTTP / Runtime vertical

The reference run proves:

```text
3 accepted daytime approach Experiences
-> fixed sleep cycle and S1 window
-> transit to safe Plaza
-> bounded Sleep completion
-> S2 profiles
-> S3 Deep Similarity
-> one sourced shadow CandidateRelation
```

Transit approaches may be admitted to raw History, but they are later than the
fixed window and cannot become candidate sources for that cycle. Sleep with only
two prior records ends as `INSUFFICIENT_EVIDENCE`.

The tests also retain the original default-off path: bounded Sleep recovers the
body while reporting `consolidation=not_run`. S4 changes no action policy,
World truth, canonical comparison, `E`, `H`, `theta_eff`, `M_delta`, `M_B'`, or
T1 authority.
